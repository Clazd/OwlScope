.env
.env.*
!.env.example

# Personal application state. Keep the directory skeleton only; real personas,
# prompts, sources, drafts, and run traces must never enter the public repo.
/data/**
!/data/.gitkeep
!/data/**/
!/data/**/.gitkeep

node_modules
.next

# Fonts are copied from node_modules on postinstall; never commit them.
/public/fonts

# build + tooling noise
/out
*.tsbuildinfo
next-env.d.ts
.DS_Store
npm-debug.log*
